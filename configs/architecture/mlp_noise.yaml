# MLP Architecture Configuration for CFM with Energy Score
_target_: architecture.mlp.MLP

# Input dimensions: [x_t, t, noise]
in_dims: 
  - ${dataset.data_dim} 
  - 1
  - ${model.noise_dim}
hidden_dim: 64
out_dim: ${dataset.data_dim}
layers: 3 